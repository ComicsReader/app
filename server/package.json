{
  "name": "comics-reader-server",
  "version": "0.0.1",
  "description": "The proxy server for ComicsReader app.",
  "main": "dist/app.js",
  "scripts": {
    "dev": "babel-node src/app.js",
    "prepublish": "npm run build",
    "build": "babel src --presets babel-preset-es2015 --out-dir dist"
  },
  "author": "Yukai Huang",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.15.2",
    "comics-dm5": "^0.0.2",
    "dotenv": "^2.0.0",
    "express": "^4.14.0"
  },
  "engines": {
    "node": "6.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0"
  }
}
